{
  "name": "ecommerce-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev --prefix client\" \"npm run start --prefix server\"",
    "build": "echo \"Add your build steps here if needed\"",
    "start": "echo \"Use 'npm run dev' to start both client and server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
